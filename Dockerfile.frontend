FROM python:3.10-slim

WORKDIR /app

COPY requirements-frontend.txt .
RUN pip install --no-cache-dir -r requirements-frontend.txt

COPY frontend/ ./frontend/

EXPOSE 8080

CMD uvicorn frontend.server:app --host 0.0.0.0 --port ${PORT:-8080}